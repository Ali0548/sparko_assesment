
export enum NotificationType {
    ALERT = 'ALERT',
    MESSAGE = 'MESSAGE',
    REMINDER = 'REMINDER'
}

export enum NotificationActionResponse {
    SUCCESS = 'Process is completed Successfully',
    ERROR = 'Something Went Wrong'
}

export enum Collections {
    NOTIFICATIONS = 'notifications'
}

export enum ToastMessage {
    SUCCESS = 'success',
    ERROR = 'error',
    INFO = 'info',
    WARN = 'warn'
}
